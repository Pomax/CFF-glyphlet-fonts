/**
 * common table layout for the Advanced Typographic Tables GPOS, GSUB, GDEF, JSTF and BASE
 */
define(["../../struct"], function(Table){
  "use strict";

  var common = function() {};

  common.prototype = new Table([]);

  return common;
});
